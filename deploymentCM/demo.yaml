---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-world-CM
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - http:
      paths:
      - path: /helloCM
        pathType: Prefix
        backend:
          service:
            name: hello-world-CM
            port:
              number: 80

---
apiVersion: v1
kind: Service
metadata:
  name: hello-world-CM
spec:
  ports:
    - port: 80
      protocol: TCP
  selector:
    app:  hello-world-CM

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-nginxCM
spec:
  selector:
    matchLabels:
      app: hello-world-CM
  replicas: 3
  template:
    metadata:
      labels:
        app: hello-world-CM
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: hello-world-volumeCM
          mountPath: /usr/share/nginx/html/helloCM
      volumes:
      - name: hello-world-volumeCM
        configMap:
          name: hello-world-CM