pipeline {
    environment {
        app = ''
    }
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    app = docker.build("starwit-app")
                }
            }
        }
    }
}