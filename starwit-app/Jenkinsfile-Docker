pipeline {
  agent any
  environment {
    DOCKER_CERT_PATH = credentials('docker-hub-credentials')
  }
  stages {
    stage('foo') {
      steps {
        sh "docker version" // DOCKER_CERT_PATH is automatically picked up by the Docker client
      }
    }
  }
}